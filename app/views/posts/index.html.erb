<p style="color: green"><%= notice %></p>

<h1>Posts</h1>

<button class="btn-primary"><%= link_to "New post", new_post_path %></button>

<div id="posts" class="d-flex flex-wrap mt-3 justify-content-center container-fluid-lg">
  <% @posts.each do |post| %>
    <div class="card col-md-6 col-lg-6 mx-1 mt-4">
      <%= image_tag post.image %>
      <div class="card-body">
        <h2><%= post.titre %></h2>
        <p class="text-truncate"><%= post.contenu %></p>
        <p><%= post.réalisateur %></p>
        <p><%= post.genre %></p>
        <p><%= post.sortie %></p>
      </div>
      <hr/>
      <div class="card-body">
        <p><%= post.auteur %> • <%= post.date %></p>
        <p class="text-light text-decoration-none"><button type="button" class="btn btn-dark"><%= link_to "Show this post", post %></button></p>
      </div>
    </div>
  <% end %>
</div>
